# Orbitport SpaceComputer API credentials (optional)
# Without these, cTRNG is fetched from the IPFS beacon (no credentials needed).
# With these, the SDK tries the Orbitport API first, with IPFS as fallback.
ORBITPORT_CLIENT_ID=your_client_id_here
ORBITPORT_CLIENT_SECRET=your_client_secret_here
